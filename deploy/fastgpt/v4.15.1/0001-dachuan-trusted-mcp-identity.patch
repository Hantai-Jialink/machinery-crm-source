diff --git a/packages/service/core/app/mcp.ts b/packages/service/core/app/mcp.ts
index f896e37..00c8b16 100644
--- a/packages/service/core/app/mcp.ts
+++ b/packages/service/core/app/mcp.ts
@@ -14,6 +14,8 @@ import { UserError } from '@fastgpt/global/common/error/utils';
 import $RefParser from '@apidevtools/json-schema-ref-parser';
 import { getLogger, LogCategories } from '../../common/logger';
 import { isInternalAddress, PRIVATE_URL_TEXT } from '../../common/system/utils';
+import { getTrustedMcpHeaders } from '../workflow/utils/context';
+import { randomUUID } from 'node:crypto';
 
 const logger = getLogger(LogCategories.MODULE.APP.MCP_TOOLS);
 
@@ -212,6 +214,22 @@ const getErrorMessage = (error: unknown) => {
   return String(error);
 };
 
+export const createMcpRequestHeaders = (
+  configuredHeaders: Record<string, any>,
+  createRequestId: () => string = randomUUID
+) => {
+  const requestHeaders = Object.fromEntries(
+    Object.entries(configuredHeaders).filter(
+      ([name]) => !name.toLowerCase().startsWith('x-dachuan-')
+    )
+  );
+  const trustedHeaders = getTrustedMcpHeaders();
+  requestHeaders['x-dachuan-request-id'] =
+    trustedHeaders['x-dachuan-request-id'] || createRequestId();
+  Object.assign(requestHeaders, trustedHeaders);
+  return requestHeaders;
+};
+
 export class MCPClient {
   private client: Client;
   private url: string;
@@ -244,6 +262,7 @@ export class MCPClient {
   private async doConnect(): Promise<Client> {
     await assertMCPUrlNotInternal(this.url);
     const safeFetch = createMcpSafeFetch();
+    const requestHeaders = createMcpRequestHeaders(this.headers);
 
     // 避免连接重复，强制关闭一次
     await this.client.close().catch(() => {});
@@ -253,7 +272,7 @@ export class MCPClient {
       const transport = new StreamableHTTPClientTransport(new URL(this.url), {
         fetch: safeFetch,
         requestInit: {
-          headers: this.headers
+          headers: requestHeaders
         }
       });
       await this.client.connect(transport);
@@ -268,12 +287,12 @@ export class MCPClient {
           new SSEClientTransport(new URL(this.url), {
             fetch: safeFetch,
             requestInit: {
-              headers: this.headers
+              headers: requestHeaders
             },
             eventSourceInit: {
               fetch: (url, init) => {
                 const mergedHeaders = {
-                  ...this.headers
+                  ...requestHeaders
                 };
 
                 Object.assign(mergedHeaders, headersInitToRecord(init?.headers));
diff --git a/packages/service/core/workflow/dispatch/index.ts b/packages/service/core/workflow/dispatch/index.ts
index f5700e6..79534c3 100644
--- a/packages/service/core/workflow/dispatch/index.ts
+++ b/packages/service/core/workflow/dispatch/index.ts
@@ -103,6 +103,10 @@ type Props = Omit<
   defaultSkipNodeQueue?: WorkflowDebugResponse['skipNodeQueue'];
   nodeResponseWriteConfig: WorkflowNodeResponseWriteConfig;
   agentSandboxPrepareActions?: AgentSandboxPrepareAction[];
+  trustedMcpIdentity?: {
+    assertion: string;
+    requestId: string;
+  };
 };
 type NodeResponseType = DispatchNodeResultType<{
   [key: string]: any;
@@ -261,7 +265,8 @@ export async function dispatchWorkFlow({
     runWithContext(
       {
         queryUrlTypeMap: {},
-        mcpClientMemory: {}
+        mcpClientMemory: {},
+        trustedMcpIdentity: data.trustedMcpIdentity
       },
       (ctx) => {
         runWorkflow({
diff --git a/packages/service/core/workflow/utils/context.ts b/packages/service/core/workflow/utils/context.ts
index ded706c..171a598 100644
--- a/packages/service/core/workflow/utils/context.ts
+++ b/packages/service/core/workflow/utils/context.ts
@@ -9,6 +9,10 @@ import type { MCPClient } from '../../app/mcp';
 type ContextType = {
   queryUrlTypeMap: Record<string, ChatFileTypeEnum>;
   mcpClientMemory: Record<string, MCPClient>;
+  trustedMcpIdentity?: {
+    assertion: string;
+    requestId: string;
+  };
 };
 
 export const WorkflowContext = new AsyncLocalStorage<ContextType>();
@@ -24,6 +28,15 @@ export const getWorkflowContext = (): ContextType => {
   return WorkflowContext.getStore()!;
 };
 
+export const getTrustedMcpHeaders = (): Record<string, string> => {
+  const identity = WorkflowContext.getStore()?.trustedMcpIdentity;
+  if (!identity) return {};
+  return {
+    'x-dachuan-user-assertion': identity.assertion,
+    'x-dachuan-request-id': identity.requestId
+  };
+};
+
 export const updateWorkflowContextVal = (val: Partial<ContextType>) => {
   const context = getWorkflowContext();
   if (context) {
diff --git a/packages/service/test/core/app/mcp.test.ts b/packages/service/test/core/app/mcp.test.ts
index b93be41..2900372 100644
--- a/packages/service/test/core/app/mcp.test.ts
+++ b/packages/service/test/core/app/mcp.test.ts
@@ -24,6 +24,7 @@ import { StreamableHTTPError } from '@modelcontextprotocol/sdk/client/streamable
 import {
   MCPClient,
   assertMCPUrlNotInternal,
+  createMcpRequestHeaders,
   createMcpSafeFetch,
   getMCPChildren
 } from '../../../core/app/mcp';
@@ -90,6 +91,22 @@ const getReachablePrivateHost = () => {
 describe('MCPClient', () => {
   const config = { url: 'https://example.com/mcp', headers: { Authorization: 'Bearer test' } };
 
+  it('creates a request id for admin discovery without accepting configured user identity', () => {
+    const headers = createMcpRequestHeaders(
+      {
+        Authorization: 'Bearer service-key',
+        'X-Dachuan-User-Assertion': 'untrusted-static-assertion',
+        'X-Dachuan-Request-Id': 'untrusted-static-request'
+      },
+      () => 'admin-discovery-request'
+    );
+
+    expect(headers).toEqual({
+      Authorization: 'Bearer service-key',
+      'x-dachuan-request-id': 'admin-discovery-request'
+    });
+  });
+
   describe('assertMCPUrlNotInternal', () => {
     it('should reject localhost MCP endpoints', async () => {
       await expect(assertMCPUrlNotInternal('http://localhost:3000/mcp')).rejects.toBe(
diff --git a/packages/service/test/core/workflow/utils/context.test.ts b/packages/service/test/core/workflow/utils/context.test.ts
index c94f22c..aff625f 100644
--- a/packages/service/test/core/workflow/utils/context.test.ts
+++ b/packages/service/test/core/workflow/utils/context.test.ts
@@ -3,6 +3,7 @@ import {
   parseUrlToFileType,
   runWithContext,
   getWorkflowContext,
+  getTrustedMcpHeaders,
   updateWorkflowContextVal
 } from '../../../../core/workflow/utils/context';
 import { ChatFileTypeEnum } from '@fastgpt/global/core/chat/constants';
@@ -57,6 +58,38 @@ describe('WorkflowContext', () => {
         });
       });
     });
+
+    it('keeps trusted MCP identity headers isolated across concurrent workflows', async () => {
+      const run = (assertion: string, requestId: string, delay: number) =>
+        new Promise<Record<string, string>>((resolve) => {
+          runWithContext(
+            {
+              ...createWorkflowContext({}),
+              trustedMcpIdentity: { assertion, requestId }
+            },
+            async () => {
+              await new Promise((done) => setTimeout(done, delay));
+              resolve(getTrustedMcpHeaders());
+            }
+          );
+        });
+
+      const expected = Array.from({ length: 48 }, (_, index) => ({
+        assertion: `assertion-user-${index % 4}-tab-${index % 3}`,
+        requestId: `request-user-${index % 4}-tab-${index % 3}-call-${index}`,
+        delay: (47 - index) % 7
+      }));
+      const actual = await Promise.all(
+        expected.map((item) => run(item.assertion, item.requestId, item.delay))
+      );
+
+      actual.forEach((headers, index) => {
+        expect(headers).toEqual({
+          'x-dachuan-user-assertion': expected[index].assertion,
+          'x-dachuan-request-id': expected[index].requestId
+        });
+      });
+    });
   });
 
   describe('updateWorkflowContextVal', () => {
diff --git a/projects/app/src/pages/api/v1/chat/completions.ts b/projects/app/src/pages/api/v1/chat/completions.ts
index a91f5c4..0afaf81 100644
--- a/projects/app/src/pages/api/v1/chat/completions.ts
+++ b/projects/app/src/pages/api/v1/chat/completions.ts
@@ -318,6 +318,19 @@ async function handler(req: NextApiRequest, res: NextApiResponse) {
       void titleSender.start();
     }
 
+    const trustedMcpIdentity = (() => {
+      const assertion = req.headers['x-dachuan-user-assertion'];
+      const requestId = req.headers['x-dachuan-request-id'];
+      if (
+        typeof assertion !== 'string' ||
+        typeof requestId !== 'string' ||
+        !/^[A-Za-z0-9][A-Za-z0-9._:-]{7,127}$/.test(requestId)
+      ) {
+        return undefined;
+      }
+      return { assertion, requestId };
+    })();
+
     /* start flow controller */
     const {
       flowUsages,
@@ -364,7 +377,8 @@ async function handler(req: NextApiRequest, res: NextApiResponse) {
       nodeResponseWriteConfig: {
         persistToDb: preparedRound.shouldPersistChatRound,
         retainInMemory: shouldCollectFinalResponseData
-      }
+      },
+      trustedMcpIdentity
     });
 
     const aiResponse: AIChatItemType & { dataId?: string } = {
