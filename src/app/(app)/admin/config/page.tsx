"use client";
import { useEffect, useState } from "react";
export default function ConfigPage() { const [items, setItems] = useState<any[]>([]); const [error, setError] = useState(""); useEffect(() => { fetch("/api/system/settings").then((res) => res.json()).then((data) => Array.isArray(data.items) ? setItems(data.items) : setError(data.error || "配置加载失败")).catch(() => setError("配置加载失败")); }, []); return <div className="space-y-4"><div><h1 className="text-xl font-semibold">配置中心</h1><p className="text-sm text-gray-500">仅显示白名单业务配置；连接串、密码、Token、密钥和服务器控制均不会显示。</p></div>{error && <p className="text-red-600 text-sm">{error}</p>}<section className="rounded-xl border bg-white p-4"><h2 className="font-medium">提醒与打印信息</h2><p className="mt-1 text-sm text-gray-500">已保存配置 {items.length} 项。编号规则在阶段 4 实施，不在此处伪造可编辑配置。</p></section></div>; }
