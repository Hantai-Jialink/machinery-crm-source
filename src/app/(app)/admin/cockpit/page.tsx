"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
export default function AdminCockpitPage() { const [health, setHealth] = useState<any>(); const [tasks, setTasks] = useState<any[]>([]); useEffect(() => { fetch("/api/system/health").then((r) => r.json()).then(setHealth); fetch("/api/system/tasks?view=approval").then((r) => r.json()).then((data) => setTasks(data.items || [])); }, []); return <div className="space-y-4"><div><h1 className="text-xl font-semibold">管理员工作台</h1><p className="text-sm text-gray-500">关注平台状态、待处理事项与最近变更；不显示任何密钥。</p></div><div className="grid gap-3 md:grid-cols-3"><section className="rounded-xl border bg-white p-4"><b>平台状态</b><p className="mt-2 text-sm">版本：{health?.build?.version || "加载中"}</p></section><section className="rounded-xl border bg-white p-4"><b>待我审批</b><p className="mt-2 text-2xl font-semibold">{tasks.length}</p></section><section className="rounded-xl border bg-white p-4"><b>快捷入口</b><div className="mt-2 flex flex-wrap gap-2 text-sm"><Link href="/users">用户</Link><Link href="/tasks">待办</Link><Link href="/admin/health">健康</Link></div></section></div></div>; }
