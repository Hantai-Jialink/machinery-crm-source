# AGENTS.md — DachuanPro CRM/ERP 项目协作规则

## 1. 项目身份

这是山东大川重工机床股份有限公司内部使用的 DachuanPro CRM/ERP 平台。

系统不是新项目，不允许推倒重建。当前平台已经上线运行，正在从 CRM 阶段进入 ERP 二期开发。

主要使用者：
- 超级管理员
- 区域销售
- 后续 ERP 管理人员

用户不懂编程，需要 Codex 输出清晰、可执行、可回滚的中文步骤。

## 2. 当前系统目标

平台用于公司内部管理：

- 客户管理
- 产品管理
- 报价管理
- 合同管理
- 回款管理
- 发货管理
- 区域权限
- 工作台
- ERP 二期模块扩展

后续 ERP 二期开发必须基于现有 CRM/ERP 继续扩展，不允许重新初始化项目。

## 3. 绝对禁止事项

Codex 必须遵守：

1. 不允许删除正式数据。
2. 不允许清空数据库。
3. 不允许重建整个项目。
4. 不允许覆盖 uploads 上传目录。
5. 不允许删除现有 CRM 功能。
6. 不允许绕过现有权限体系。
7. 不允许未经确认执行数据库破坏性操作。
8. 不允许随意修改生产服务器配置。
9. 不允许把敏感信息写入日志。
10. 不允许在没有回滚方案的情况下给出部署建议。

如果任务可能影响正式数据，必须先提醒用户备份，并说明风险。

## 4. 开发原则

优先采用小步补丁方式：

1. 先理解现有代码结构。
2. 先列出涉及文件。
3. 再说明修改方案。
4. 最后再修改代码。
5. 修改后必须说明如何测试。
6. 涉及数据库时必须说明迁移方案和回滚方案。

除非用户明确要求，不要进行大规模重构。

## 5. 输出格式

每次接到开发任务时，先输出：

### 任务理解
用中文简要说明你理解的需求。

### 涉及范围
列出可能涉及的页面、组件、API、数据库表、权限逻辑。

### 风险判断
说明是否涉及正式数据、权限、数据库迁移、上传文件、服务器配置。

### 实施计划
分步骤说明准备怎么做。

用户确认后，再开始修改代码。

修改完成后，必须输出：

### 已修改文件
列出文件路径和修改内容。

### 数据库变更
说明是否有数据库字段、表结构、迁移脚本变化。

### 测试方法
给出用户可以实际操作的测试清单。

### 部署步骤
给出清晰步骤。

### 回滚方案
说明出问题后如何恢复。

## 6. 技术环境记忆

已知部署环境可能包括：

- 阿里云服务器
- 宝塔面板
- Nginx
- MySQL
- Node.js
- pnpm
- PM2
- Next.js
- 项目路径可能为：/opt/machinery-crm-v108-release
- 上传目录可能为：/opt/machinery-crm-uploads
- 备份目录可能为：/opt/crm-backups

注意：这些路径需要在执行前再次确认，不要盲目假设。

## 7. 服务器部署原则

部署前必须建议用户执行：

1. 备份项目目录。
2. 备份数据库。
3. 确认当前 PM2 服务名称。
4. 确认 Nginx 配置。
5. 确认环境变量。
6. 确认上传目录不被覆盖。

如果需要执行命令，必须解释每条命令的作用。

不要让用户执行危险的一行式命令。

## 8. ERP 二期开发规则

ERP 二期必须保护现有 CRM 逻辑。

新增模块应优先采用独立页面、独立 API、独立数据表或清晰关联表，避免直接破坏已有客户、合同、产品、报价、回款数据。

涉及以下模块时必须特别谨慎：

- 合同与订单联动
- 回款与合同状态
- 发货与物流记录
- 库存数量
- 产品/配件/选配项
- 审批流程
- 超级管理员权限

## 9. 权限规则

任何新增页面或接口都必须考虑角色权限：

- 超级管理员可以查看和管理全部数据。
- 区域销售只能查看自己权限范围内的数据。
- 后续 ERP 角色权限需要明确设计。

不要只做前端隐藏，后端 API 也必须校验权限。

## 10. GitHub 和提交规则

每次修改完成后，输出建议 commit message。

commit message 建议使用中文或中英结合，格式：

feat: 新增 ERP 二期 xxx 功能
fix: 修复 xxx 页面报错
refactor: 优化 xxx 逻辑
chore: 调整部署配置

提交前必须说明是否已运行测试。

## 11. 沟通方式

用户不是程序员。

回答必须：
- 用中文；
- 分步骤；
- 不要省略关键操作；
- 不要直接给一堆代码不解释；
- 遇到不确定信息时先说明需要确认什么；
- 对高风险操作必须明确提醒。

## 项目上下文文件

开始任何开发任务前，优先阅读：

- docs/ai-context/01_project_overview.md
- docs/ai-context/02_erp_phase2_context.md

涉及部署、服务器、数据库迁移时，还必须阅读：

- docs/ai-context/03_deployment_notes.md

涉及代码审核、上线前检查时，还必须阅读：

- docs/ai-context/05_review_checklist.md

## 12. AGENTS.md 维护规则

AGENTS.md 是本仓库的长期协作规则文件，只记录稳定规则，不记录临时任务、临时报错、单次部署记录或短期开发计划。

除非出现以下情况，否则不要修改 AGENTS.md：

1. 项目协作方式发生变化；
2. 新增长期禁止事项；
3. 新增长期部署原则；
4. 新增长期测试规则；
5. 新增长期权限规则；
6. 用户明确要求更新 AGENTS.md。

以下内容不得写入 AGENTS.md，应写入对应上下文文件：

- 当前开发进度：写入 docs/ai-context/00_current_status.md
- ERP 二期阶段说明：写入 docs/ai-context/02_erp_phase2_context.md
- 服务器部署变化：写入 docs/ai-context/03_deployment_notes.md
- 当前任务说明：写入单次任务提示词或 issue/PR 描述
- 重要架构决策：追加到 docs/ai-context/06_decisions_log.md

每次开发任务开始前，优先阅读 AGENTS.md。
涉及当前开发状态时，再阅读 docs/ai-context/00_current_status.md。
涉及 ERP 二期时，再阅读 docs/ai-context/02_erp_phase2_context.md。
涉及部署、数据库迁移、服务器操作时，再阅读 docs/ai-context/03_deployment_notes.md。
涉及上线审核时，再阅读 docs/ai-context/05_review_checklist.md。
